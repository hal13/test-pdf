<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :email_for_index %>
    <%= f.text_field :email_for_index %>
  </div>

  <div class="field">
    <%= f.label :family_name %>
    <%= f.text_field :family_name %>
  </div>

  <div class="field">
    <%= f.label :given_name %>
    <%= f.text_field :given_name %>
  </div>

  <div class="field">
    <%= f.label :hashed_password %>
    <%= f.text_field :hashed_password %>
  </div>

  <div class="field">
    <%= f.label :suspended %>
    <%= f.check_box :suspended %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
